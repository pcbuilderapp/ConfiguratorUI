
import 'componentref.dart';

class Configuration {
  ComponentRef motherboard;
  ComponentRef cpu;
  ComponentRef gpu;
  ComponentRef memory;
  ComponentRef storage;
  ComponentRef psu;
  ComponentRef casing;
}